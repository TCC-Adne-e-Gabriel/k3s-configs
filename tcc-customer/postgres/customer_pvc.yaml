apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: customer-volume-claim
  labels:
    app: customer-db
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi