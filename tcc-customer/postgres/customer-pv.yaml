apiVersion: v1
kind: PersistentVolume
metadata:
  name: customer-volume
  labels:
    type: local
    app: customer-db
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/postgresql