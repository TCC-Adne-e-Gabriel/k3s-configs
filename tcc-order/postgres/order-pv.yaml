apiVersion: v1
kind: PersistentVolume
metadata:
  name: order-volume
  labels:
    type: local
    app: order-db
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/orderdb